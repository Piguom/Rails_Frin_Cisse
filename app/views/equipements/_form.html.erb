<%= form_for(@equipement) do |f| %>
  <% if @equipement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@equipement.errors.count, "error") %> prohibited this equipement from being saved:</h2>

      <ul>
      <% @equipement.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field form-group">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field form-group">
    <%= f.label :description %><br>
    <%= f.text_area :description %>
  </div>
  <div class='field form-group'>
     <%= f.label :sport %><br>
    
    <%= f.select :sport_id , Sport.all.map{|sport| [sport.name,sport.id]} %>
  </div>
  <div class="actions btn btn-default">
    <%= f.submit %>
  </div>
<% end %>
